# Control Character Intent Test Area

This directory stores test artifacts for `control-character-intent`.

Expected first test artifacts:

- one chapter runtime-state sample;
- one CONTINUE intent-track sample;
- one ASK_USER interruption sample;
- one RESUME update sample;
- one prose-fragment handoff sample for `generate-chapter-prose-draft`.

Do not use these test artifacts as system source material for generation unless a test explicitly says so.
